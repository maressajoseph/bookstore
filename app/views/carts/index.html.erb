<% @cart.each do |product| %>
  <%= product.title %>
  <%= product.price %>
  <%= image_tag(product.image) %>

<%= link_to "Remove from Cart", cart_path(product), method: :delete, data: {confirm: "Are you sure?"} %>
<% end %>
